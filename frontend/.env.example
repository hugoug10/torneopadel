# Opcional: solo necesario si el frontend y la API NO se sirven desde el mismo origen.
# Con Cloudflare Pages Functions (functions/) no hace falta definir esta variable.
# VITE_API_URL=https://tu-dominio.pages.dev/api
